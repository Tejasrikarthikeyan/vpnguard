<UserControl x:Class="IPsecSecurityAnalyzer.Views.IpsecAnalysisView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1000">

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="24">
        <StackPanel MaxWidth="1200" HorizontalAlignment="Stretch">

            <!-- Title -->
            <TextBlock Text="IPsec Analysis" Style="{StaticResource TitleHeaderStyle}" />
            <TextBlock Text="Detailed cryptographic transforms, IKE negotiation phases, and ESP tunnel parameters." Style="{StaticResource SubtitleHeaderStyle}" Margin="0,4,0,20" />

            <!-- IKE Information Section -->
            <Border Style="{StaticResource SocCardBorderStyle}" Margin="0,0,0,20">
                <StackPanel>
                    <Grid Margin="0,0,0,16">
                        <TextBlock Text="IKE Information (Internet Key Exchange)" Style="{StaticResource CardHeaderStyle}" />
                        <Path Data="{StaticResource IconIpsec}" Fill="{StaticResource BrushAccentCyan}" Width="16" Height="16" Stretch="Uniform" HorizontalAlignment="Right" />
                    </Grid>

                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="220" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <!-- IKE Version -->
                        <TextBlock Grid.Row="0" Grid.Column="0" Text="IKE Version" Foreground="{StaticResource BrushTextMuted}" FontSize="13" Margin="0,6" />
                        <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding AnalysisResult.DisplayIkeVersion}" Foreground="{StaticResource BrushTextPrimary}" FontSize="13" FontWeight="SemiBold" Margin="0,6" />

                        <!-- Exchange Type -->
                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Exchange Type" Foreground="{StaticResource BrushTextMuted}" FontSize="13" Margin="0,6" />
                        <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding AnalysisResult.DisplayExchangeType}" Foreground="{StaticResource BrushTextPrimary}" FontSize="13" Margin="0,6" />

                        <!-- Authentication Method -->
                        <TextBlock Grid.Row="2" Grid.Column="0" Text="Authentication" Foreground="{StaticResource BrushTextMuted}" FontSize="13" Margin="0,6" />
                        <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding AnalysisResult.DisplayAuthMethod}" Foreground="{StaticResource BrushTextPrimary}" FontSize="13" Margin="0,6" />

                        <!-- Key Exchange -->
                        <TextBlock Grid.Row="3" Grid.Column="0" Text="Key Exchange" Foreground="{StaticResource BrushTextMuted}" FontSize="13" Margin="0,6" />
                        <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding AnalysisResult.DisplayEncryption}" Foreground="{StaticResource BrushTextPrimary}" FontSize="13" Margin="0,6" />

                        <!-- DH Group -->
                        <TextBlock Grid.Row="4" Grid.Column="0" Text="DH Group" Foreground="{StaticResource BrushTextMuted}" FontSize="13" Margin="0,6" />
                        <TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding AnalysisResult.DisplayDhGroup}" Foreground="{StaticResource BrushTextPrimary}" FontSize="13" Margin="0,6" />
                    </Grid>
                </StackPanel>
            </Border>

            <!-- IPsec / ESP Information Section -->
            <Border Style="{StaticResource SocCardBorderStyle}" Margin="0,0,0,20">
                <StackPanel>
                    <Grid Margin="0,0,0,16">
                        <TextBlock Text="IPsec / ESP Information (Encapsulating Security Payload)" Style="{StaticResource CardHeaderStyle}" />
                        <Path Data="{StaticResource IconLock}" Fill="{StaticResource BrushAccentBlue}" Width="16" Height="16" Stretch="Uniform" HorizontalAlignment="Right" />
                    </Grid>

                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="220" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <!-- Protocol -->
                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Protocol" Foreground="{StaticResource BrushTextMuted}" FontSize="13" Margin="0,6" />
                        <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding AnalysisResult.DisplayProtocol}" Foreground="{StaticResource BrushTextPrimary}" FontSize="13" Margin="0,6" />

                        <!-- Mode -->
                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Mode" Foreground="{StaticResource BrushTextMuted}" FontSize="13" Margin="0,6" />
                        <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding AnalysisResult.DisplayIpsecMode}" Foreground="{StaticResource BrushTextPrimary}" FontSize="13" Margin="0,6" />

                        <!-- Encryption Algorithm -->
                        <TextBlock Grid.Row="2" Grid.Column="0" Text="Encryption" Foreground="{StaticResource BrushTextMuted}" FontSize="13" Margin="0,6" />
                        <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding AnalysisResult.DisplayEncryption}" Foreground="{StaticResource BrushTextPrimary}" FontSize="13" Margin="0,6" />

                        <!-- Integrity Algorithm -->
                        <TextBlock Grid.Row="3" Grid.Column="0" Text="Integrity" Foreground="{StaticResource BrushTextMuted}" FontSize="13" Margin="0,6" />
                        <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding AnalysisResult.DisplayIntegrity}" Foreground="{StaticResource BrushTextPrimary}" FontSize="13" Margin="0,6" />

                        <!-- SPI -->
                        <TextBlock Grid.Row="4" Grid.Column="0" Text="SPI" Foreground="{StaticResource BrushTextMuted}" FontSize="13" Margin="0,6" />
                        <TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding AnalysisResult.DisplaySpi}" Foreground="{StaticResource BrushTextPrimary}" FontSize="13" Margin="0,6" />

                        <!-- Lifetime -->
                        <TextBlock Grid.Row="5" Grid.Column="0" Text="Lifetime" Foreground="{StaticResource BrushTextMuted}" FontSize="13" Margin="0,6" />
                        <TextBlock Grid.Row="5" Grid.Column="1" Text="{Binding AnalysisResult.DisplayKeyLifetime}" Foreground="{StaticResource BrushTextPrimary}" FontSize="13" Margin="0,6" />

                        <!-- PFS -->
                        <TextBlock Grid.Row="6" Grid.Column="0" Text="PFS" Foreground="{StaticResource BrushTextMuted}" FontSize="13" Margin="0,6" />
                        <TextBlock Grid.Row="6" Grid.Column="1" Text="{Binding AnalysisResult.DisplayPfs}" Foreground="{StaticResource BrushTextPrimary}" FontSize="13" Margin="0,6" />

                        <!-- Replay Protection -->
                        <TextBlock Grid.Row="7" Grid.Column="0" Text="Replay Protection" Foreground="{StaticResource BrushTextMuted}" FontSize="13" Margin="0,6" />
                        <TextBlock Grid.Row="7" Grid.Column="1" Text="{Binding AnalysisResult.DisplayReplayProtection}" Foreground="{StaticResource BrushTextPrimary}" FontSize="13" Margin="0,6" />
                    </Grid>
                </StackPanel>
            </Border>

            <!-- Traffic Information Section -->
            <Border Style="{StaticResource SocCardBorderStyle}">
                <StackPanel>
                    <Grid Margin="0,0,0,16">
                        <TextBlock Text="Traffic Information" Style="{StaticResource CardHeaderStyle}" />
                        <Path Data="{StaticResource IconPcap}" Fill="{StaticResource BrushAccentCyan}" Width="16" Height="16" Stretch="Uniform" HorizontalAlignment="Right" />
                    </Grid>

                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="220" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <!-- Source -->
                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Source" Foreground="{StaticResource BrushTextMuted}" FontSize="13" Margin="0,6" />
                        <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding AnalysisResult.DisplaySourceAddress}" Foreground="{StaticResource BrushTextPrimary}" FontSize="13" Margin="0,6" />

                        <!-- Destination -->
                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Destination" Foreground="{StaticResource BrushTextMuted}" FontSize="13" Margin="0,6" />
                        <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding AnalysisResult.DisplayDestAddress}" Foreground="{StaticResource BrushTextPrimary}" FontSize="13" Margin="0,6" />

                        <!-- Packet Count -->
                        <TextBlock Grid.Row="2" Grid.Column="0" Text="Packet Count" Foreground="{StaticResource BrushTextMuted}" FontSize="13" Margin="0,6" />
                        <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding AnalysisResult.DisplayPacketCount}" Foreground="{StaticResource BrushTextPrimary}" FontSize="13" Margin="0,6" />

                        <!-- Traffic Type -->
                        <TextBlock Grid.Row="3" Grid.Column="0" Text="Traffic Type" Foreground="{StaticResource BrushTextMuted}" FontSize="13" Margin="0,6" />
                        <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding AnalysisResult.DisplayTrafficType}" Foreground="{StaticResource BrushTextPrimary}" FontSize="13" Margin="0,6" />
                    </Grid>
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</UserControl>
